# vpner

This project provides a VPN manager with support for running Xray chains.
Use the `make.sh` script to cross-compile the `vpnerd` daemon for a MIPS-based router such as Keenetic. The built binary is compressed with `upx`.
